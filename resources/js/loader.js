const loader = document.querySelector('#loader');

window.addEventListener('DOMContentLoaded', () => {
    loader.classList.add('hide-loader')

})

// TODO: Fixer la visibilité du loader en adéquation avec le wire:navigate